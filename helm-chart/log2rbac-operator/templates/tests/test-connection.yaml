apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "log2rbac-operator.fullname" . }}-test-connection"
  labels:
    {{- include "log2rbac-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "log2rbac-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
